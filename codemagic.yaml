workflows:
  android-workflow:
    name: Android Build
    max_build_duration: 60
    environment:
      flutter: stable
    scripts:
      - name: Build APK
        script: |
          flutter create --project-name daftar --org com.daftar .
          flutter pub get
          flutter build apk --release
    artifacts:
      - build/**/outputs/**/*.apk
